package com.maksubov.locator.repository

import android.location.Location

class LocationRepository {

    suspend fun addNewLocation(location: Location){
//        val entity = location.toLocationEntity()
//        dao.addLocation(entity)
    }
}